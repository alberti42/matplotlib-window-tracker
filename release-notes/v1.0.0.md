## Notes
- First stable release.
- Provides tagged Matplotlib figures via `subplots(tag, ...)` and a nonblocking refresh helper via `show(nonblocking=True)`.
- Includes best-effort backend selection with `ensure_backend()` and a diagnostics CLI (`mpl-nonblock-diagnose`).

## Compatibility
- Python >= 3.10
